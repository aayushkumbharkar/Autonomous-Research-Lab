version: 3
systemUnderTest:
  service:
    definitions:
      - definition:
          source:
            filesystem:
              directory: .
          specs:
            - openapi.yaml
    runOptions:
      openapi:
        type: test
        baseUrl: http://localhost:8000
        actuatorUrl: http://localhost:8000/actuator/mappings
specmatic:
  settings:
    backwardCompatibility:
      strictMode: true
      baseBranch: origin/main
    mock:
      delayInMilliseconds: 1000
      generative: true
    test:
      timeoutInMilliseconds: 1000
      schemaResiliencyTests: all
  governance:
    report:
      formats:
        - ctrf
        - html
      outputDirectory: build/reports/specmatic
    successCriteria:
      enforce: true
      maxMissedOperationsInSpec: 0
      minCoveragePercentage: 100
  license:
    path: "{SPECMATIC_LICENSE_PATH:./license.txt}"
